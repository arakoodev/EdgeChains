# AWS Comprehend Redaction Example

This example shows how to redact PII from a prompt before sending it to an EdgeChains AI endpoint.

## Run

```bash
npm install
npm run build
npm start
```

The example uses the default AWS SDK credential provider chain. Set `AWS_REGION` before running it, and configure AWS credentials outside of source code.
