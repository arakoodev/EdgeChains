FROM cypress/included:13.14.1

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy source code and Cypress tests
COPY . .

# Set the base URL for the application
ENV CYPRESS_BASE_URL=http://host.docker.internal:3000

# Run Cypress tests
CMD ["npx", "cypress", "run"]